# Copy to .env.local for `npm run dev` / local builds.
# Set VITE_AUTH_USERS at build time (e.g. in .env or .env.local).
# JSON array of { username, password } objects (plaintext; still visible in the built JS bundle).
VITE_AUTH_USERS=[{"username":"admin","password":"changeme"},{"username":"viewer","password":"viewonly"}]
